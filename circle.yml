general:
  artifacts:
    - "build"

dependencies:
    override:
    - rm -f ~/.gitconfig
    cache_directories:
    - ~/.m2/repository
    - ~/.gradle
    - ~/.go

test:
  override:
    - ./gradlew --stacktrace package
