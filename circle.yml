general:
  artifacts:
    - "build"

dependencies:
    override:
    - exit 0
    cache_directories:
    - ~/.m2/repository
    - ~/.gradle
    - ~/.go

test:
  override:
    - ./gradlew --stracktrace package
