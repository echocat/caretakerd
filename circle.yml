general:
  artifacts:
    - "build"

dependencies:
    override:
    - true
    cache_directories:
    - ~/.m2/repository
    - ~/.gradle
    - ~/.go

test:
  override:
    - ./gradlew --stracktrace package
